const mydb = "perpus_db";
use(mydb);

// Find specific
// var data = db.buku.findOne({ _id: 8 }, { _id: 0 });
// data.categories[1];

// Find return one document
// db.buku.find({ _id: 10 });

// FInd categories "Java"
// db.buku.find({ categories: "Java", _id: 28 });
